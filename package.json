{
  "name": "node-pagespeed",
  "version": "1.0.0",
  "description": "Takes 2 command line parameters and makes requests to Google pagespeed api.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "to easy to introduce myself",
  "license": "ISC",
  "dependencies": {
    "isomorphic-fetch": "^2.2.1"
  }
}
